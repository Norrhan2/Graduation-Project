# Streamlit secrets — copy to secrets.toml locally, OR paste into
# Streamlit Community Cloud → your app → Settings → Secrets.
#
# Point the frontend at your deployed backend (no trailing slash):
TARIIKHNA_API_URL = "https://your-backend.onrender.com"
