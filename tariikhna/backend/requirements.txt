fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlmodel==0.0.22
pydantic-settings==2.5.2
httpx==0.27.2
fal-client==0.5.0
python-dotenv==1.0.1

# Only needed if you set LLM_MODE=local in .env (i.e. running the model
# directly inside this backend process, on a GPU machine).
# Comment these out if you're using LLM_MODE=remote.
# unsloth
# torch
# transformers